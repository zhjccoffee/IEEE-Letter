\documentclass[journal,twoside,web]{ieeecolor}
\usepackage{generic}
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{algorithm,algorithmic}
\usepackage{hyperref}
\hypersetup{hidelinks=true}
\usepackage{textcomp}
\usepackage{array}


\newcolumntype{L}[1]{>{\raggedright\arraybackslash}m{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
  % 左对齐+自动伸缩

\usepackage{tabularx} 
\usepackage{multirow}
\usepackage{booktabs}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\markboth{\hskip25pc LOW-DIMENSIONAL REFLEX-INFORMED DRL FOR GAIT}
{Author \MakeLowercase{\textit{et al.}}: Low-Dimensional Reflex-Informed DRL for Musculoskeletal Gait}
\begin{document}
\title{Low-Dimensional Reflex-Informed Deep Reinforcement Learning for Musculoskeletal Gait Generation}
\author{First A. Author, \IEEEmembership{Fellow, IEEE}, Second B. Author, and Third C. Author Jr., \IEEEmembership{Member, IEEE}
\thanks{This paragraph of the first footnote will contain the date on 
which you submitted your paper for review. It will also contain support 
information, including sponsor and financial support acknowledgment. For 
example, ``This work was supported in part by the U.S. Department of 
Commerce under Grant 123456.'' }
\thanks{The next few paragraphs should contain 
the authors' current affiliations, including current address and e-mail. For 
example, First A. Author is with the National Institute of Standards and 
Technology, Boulder, CO 80305 USA (e-mail: author@boulder.nist.gov). }
\thanks{Second B. Author Jr. was with Rice University, Houston, TX 77005 USA. He is 
now with the Department of Physics, Colorado State University, Fort Collins, 
CO 80523 USA (e-mail: author@lamar.colostate.edu).}
\thanks{Third C. Author is with 
the Electrical Engineering Department, University of Colorado, Boulder, CO 
80309 USA, on leave from the National Research Institute for Metals, 
Tsukuba, Japan (e-mail: author@nrim.go.jp).}}

\maketitle

\begin{abstract}
Deep reinforcement learning (DRL) has shown promise for controlling
musculoskeletal locomotion, but direct muscle-level control requires
exploration in a high-dimensional and highly redundant action space. This
letter investigates a low-dimensional reflex-informed DRL pipeline for musculoskeletal
gait generation. Instead of directly commanding all muscle
excitations, the proposed residual reflex pipeline uses an existing
reflex-based controller as a structured neuromechanical prior inspired by
spinal sensory feedback pathways, and trains a
DRL policy to modulate four selected reflex parameters through residual
actions. The policy therefore operates in a compact reflex-parameter space
while preserving the biomechanical feedback structure embedded in the
reflex controller. The pipeline is validated in a Hyfydy-based planar
musculoskeletal model using target-speed walking tasks at 0.8, 1.0, and
1.2~m/s. The proposed controller is compared with two direct-control baselines:
an end-to-end muscle-control policy and a DEP-RL policy using
DEP-style exploratory feedback. Although the direct-control baselines can
generate walking motions, their normalized gait-cycle profiles show substantial
deviations from reference gait, especially in knee kinematics and vertical
ground reaction force. In contrast, the residual reflex controller achieves
higher reference-envelope gait-cycle scores across the tested speeds and
produces more symmetric and repeatable gait cycles. These
results suggest that reducing the effective action space through residual
reflex modulation can improve the physiological plausibility of learned
musculoskeletal walking.
\end{abstract}

\begin{IEEEkeywords}
Deep reinforcement learning, gait generation, musculoskeletal simulation,
reflex control, residual reinforcement learning.
\end{IEEEkeywords}

\section{Introduction}
\label{sec:introduction}

\IEEEPARstart{H}{uman} walking is generated by the interaction of neural
feedback, muscle--tendon dynamics, skeletal mechanics, and contact with the
environment. Neuromusculoskeletal and musculoskeletal models provide a useful
platform for studying this interaction because they explicitly connect muscle
activation to joint motion and ground reaction forces. However, controlling
such models remains difficult. The system is highly overactuated, the muscle
dynamics are strongly coupled, and many different activation patterns can
produce similar task-level motion. As a result, a controller that achieves a
locomotion objective may still produce gait that is irregular, asymmetric, or
physiologically implausible.

Biologically inspired reflex controllers provide one way to address this
problem. Muscle reflex models encode local sensory--motor feedback pathways
inspired by spinal reflex mechanisms and have been shown to generate
human-like walking dynamics \cite{geyer2010}. Predictive simulation tools further make it possible to optimize and
evaluate such controllers in simulations of biological motion. Nevertheless, reflex-based
controllers typically depend on carefully selected gains, thresholds, and
offsets. These parameters are often tuned manually or optimized offline, and
their effects can be sensitive to the musculoskeletal model and task condition.

Deep reinforcement learning (DRL) offers a complementary route by learning
closed-loop policies directly from interaction with simulation. Recent work on
musculoskeletal reinforcement learning has highlighted the difficulty of
exploration in high-dimensional muscle-level action spaces. For example,
DEP-RL addresses this issue by improving embodied exploration while still
operating in the original overactuated control space \cite{deprl2023}. This
raises a related question: rather than improving exploration in the full muscle
excitation space, can the effective action space itself be reduced by learning
at the level of structured reflex modulation?

This letter investigates that question through a low-dimensional
reflex-informed DRL pipeline
for musculoskeletal walking. Instead of replacing the reflex
controller with an end-to-end policy, the proposed method keeps the reflex
controller as a low-level neuromuscular feedback prior and trains a policy to output
residual adjustments to a small set of selected reflex parameters. The role of
DRL is therefore changed from direct muscle excitation control to modulation of
an interpretable neuromechanical controller. This idea is related in spirit to
residual reinforcement learning \cite{residualrl2019}, but is applied here to
reflex-parameter modulation in a muscle-driven gait environment.

The main contributions of this letter are as follows:
\begin{enumerate}
    \item A residual reflex action representation is formulated for
    musculoskeletal gait generation. The learned policy modulates four selected
    reflex parameters instead of directly commanding all muscle excitations.
    \item The proposed pipeline is implemented in a Hyfydy-based planar
    musculoskeletal environment and compared with end-to-end muscle-level
    direct-control baselines on controlled walking tasks.
    \item The learned gaits are evaluated using normalized gait-cycle
    kinematics and vertical ground reaction force (GRF) profiles. The results
    show that residual reflex modulation improves reference gait-cycle similarity,
    especially for knee kinematics, ankle kinematics, and GRF morphology.
    \item The comparison demonstrates that successful walking alone is not
    sufficient evidence of physiologically plausible musculoskeletal gait,
    motivating gait-quality evaluation using waveform and stride-level metrics.
\end{enumerate}

The remainder of this letter is organized as follows: Section II presents the residual reflex control formulation and learning pipeline; Section III describes the controlled walking validation setup and evaluation metrics; Section IV reports and analyzes the results; and Section V concludes the paper and outlines
future work.


\section{Methodology}
\subsection{Residual-Reflex Control Architecture}
The proposed method treats the reflex controller as a structured low-level
motor prior inspired by spinal sensory feedback circuits and learns only a
compact residual modulation on top of it. As
shown in Fig.~\ref{framwork}, the musculoskeletal model returns proprioceptive
and kinematic states to a high-level DRL policy. The policy outputs a
low-dimensional residual action, which modifies selected reflex parameters
before the reflex controller generates muscle excitations. The resulting muscle
excitations are then transformed into skeletal motion and ground reaction forces
by the musculoskeletal simulator. This hierarchy changes the role of DRL from
direct muscle excitation control to reflex-parameter modulation.

\begin{figure}[!t]
\centerline{\includegraphics[width=\columnwidth]{framwork.png}}
\caption{Low-dimensional reflex-informed DRL pipeline. The learned policy does
not directly command all muscle excitations. Instead, it modulates selected
parameters of a neural-feedback-inspired reflex controller, which then produces
muscle-level stimulation for the
musculoskeletal model.}
\label{framwork}
\end{figure}

Let $\mathbf{s}_t$ denote the state observed by the policy at time $t$. The
state contains muscle-level signals, foot states, whole-body kinematics, and
joint-space variables:
\begin{equation}
\begin{aligned}
\mathbf{s}_t = [
&\mathbf{l}_t,\dot{\mathbf{l}}_t,\mathbf{f}_t,\mathbf{e}_t,
\boldsymbol{\alpha}_t,\mathbf{p}_{feet,t},\\
&\mathbf{q}_t,\dot{\mathbf{q}}_t,\theta_t,\dot{\theta}_t],
\end{aligned}
\end{equation}
where $\mathbf{l}$, $\dot{\mathbf{l}}$, and $\mathbf{f}$ are muscle fiber
lengths, velocities, and forces; $\mathbf{e}$ and $\boldsymbol{\alpha}$ are
muscle excitations and activations; $\mathbf{p}_{feet}$ represents foot
positions; $\mathbf{q}$ and $\dot{\mathbf{q}}$ are joint angles and angular
velocities; and $\theta$ is the trunk pitch angle.

\subsection{Reflex Controller}
The low-level controller is a phase-dependent reflex controller inspired by
neuromuscular feedback pathways, including muscle-spindle-like length and
velocity feedback and Golgi-tendon-organ-like force feedback. The gait cycle is divided into stance,
liftoff, swing, and landing-related subphases. In each subphase, sparse
reflex pathways map sensory feedback to target muscle stimulation. For a target
muscle $i$ and a source muscle $j$, the controller includes length, velocity,
and force feedback terms:
\begin{align}
u_{L,i\leftarrow j} &= k_{L,i\leftarrow j}
\max(0,\tilde{l}_j(t-t_D)-\tilde{l}_{0,i\leftarrow j}),\\
u_{V,i\leftarrow j} &= k_{V,i\leftarrow j}
\max(0,\tilde{v}_j(t-t_D)-\tilde{v}_{0,i\leftarrow j}),\\
u_{F,i\leftarrow j} &= k_{F,i\leftarrow j}
(\tilde{f}_j(t-t_D)-\tilde{f}_{0,i\leftarrow j}),
\end{align}
where $\tilde{l}$, $\tilde{v}$, and $\tilde{f}$ are normalized muscle length,
velocity, and force; $k_L$, $k_V$, and $k_F$ are reflex gains; and $t_D$ is a
sensorimotor delay. A reflex-like postural feedback term is also used to
couple trunk pitch errors to selected lower-limb muscles:
\begin{equation}
u_{\text{PD},i}=k_{P,i}(\theta(t-t_D)-\theta_{0,i})+
k_{D,i}\dot{\theta}(t-t_D).
\end{equation}
The total stimulation of muscle $i$ is the sum of feedforward, local feedback,
and postural feedback components:
\begin{equation}
u_i = u_{C,i}+u_{\text{PD},i}
+\sum_{j\in\mathcal{M}}(u_{L,i\leftarrow j}
+u_{V,i\leftarrow j}+u_{F,i\leftarrow j}).
\end{equation}
This reflex structure provides immediate sensory feedback and a biomechanical
inductive bias before policy learning is applied.

\subsection{Residual Action Space}
Instead of allowing the policy to directly command all muscle excitations, the
proposed controller uses a four-dimensional residual action
$\mathbf{a}_t\in\mathbb{R}^{4}$. Each action component modulates one selected
reflex parameter around its nominal value:
\begin{equation}
p_i(t)=p_{i,0}(1+\lambda a_i(t)),
\end{equation}
where $p_{i,0}$ is the nominal reflex parameter and $\lambda$ is the action
scale. In the present implementation, $\lambda=0.5$. Thus $a_i(t)=0$ recovers
the underlying reflex controller, while nonzero actions produce state-dependent
residual modulation.

\begin{table}[!t]
\caption{Selected Residual Reflex Actions}
\label{tab:residual_actions}
\centering
\resizebox{\columnwidth}{!}{
\begin{tabular}{llll}
\toprule
Action & Parameter & Pathway & Functional role \\
\midrule
$a_1$ & $k_{L,\mathrm{HAM}}$ & S11100.hamstrings.KL & Hip extension / late-swing braking \\
$a_2$ & $\tilde{l}_{0,\mathrm{ILI}}$ & S00011.iliopsoas.L0 & Hip flexion / swing initiation \\
$a_3$ & $k_{F,\mathrm{SOL}}$ & S11111.soleus.KF & Ankle support / propulsion \\
$a_4$ & $k_{F,\mathrm{VAS}}$ & S00011.vasti.KF & Knee extension / stance support \\
\bottomrule
\end{tabular}}
\end{table}

The pathway labels in Table~\ref{tab:residual_actions} follow the controller
implementation notation: the binary code after ``S'' denotes the gait subphases
in which the corresponding reflex pathway is active, and the suffix specifies
the muscle and reflex parameter being modulated.

These four parameters were selected because they act on gait-critical pathways:
hip flexion/extension for leg swing and placement, knee extension for stance
support, and ankle plantarflexion for support and propulsion. The purpose of
this selection is not to claim global optimality of the four parameters, but to
test whether a small and interpretable reflex-parameter action space can guide
DRL toward more natural gait than direct muscle-level control.

\subsection{Policy Learning}
The policy is trained with Maximum a Posteriori Policy Optimization (MPO), an
off-policy actor--critic algorithm for continuous control. The same optimizer
is used across the compared policies. Therefore, the main experimental
comparison isolates the control representation: low-dimensional
reflex-parameter modulation versus direct muscle-level action, with and without
DEP-style exploratory feedback.

At each time step, the policy samples an action from a Gaussian action
distribution conditioned on $\mathbf{s}_t$. The action is applied to the
controller, the simulator advances one step, and the transition
$(\mathbf{s}_t,\mathbf{a}_t,r_t,\mathbf{s}_{t+1})$ is stored for off-policy
updates. The base reward combines target-speed tracking ($r_{vel}$) with
regularization terms for excessive ground reaction force ($r_{grf}$), abrupt
excitation changes ($r_{smooth}$), muscle usage ($r_{musc}$), joint-limit
torques ($r_{constr}$), and non-foot self-contact ($r_{self}$):
\begin{equation}
\begin{aligned}
r_t^{base} =\,
&w_{vel}r_{vel}+w_{grf}r_{grf}+w_{smooth}r_{smooth}\\
&+w_{musc}r_{musc}+w_{constr}r_{constr}+w_{self}r_{self}.
\end{aligned}
\end{equation}
For the residual-reflex controller, additional terms regularize pelvis pitch
($r_{\theta}$), pelvis pitch rate ($r_{\dot{\theta}}$), vertical
center-of-mass velocity ($r_{com}$), and residual-action changes
($r_{\Delta a}$):
\begin{equation}
r_t = r_t^{base} + w_{\theta}r_{\theta}
+w_{\dot{\theta}}r_{\dot{\theta}}+w_{com}r_{com}+w_{\Delta a}r_{\Delta a}.
\end{equation}
\section{Experiments}
\subsection{Environments and Tasks}\label{sec:experiments}
\subsubsection{Simulation Environment}
The experiments were conducted in a two-dimensional planar musculoskeletal
model implemented with the Hyfydy physics engine \cite{hyfydy}. The model
contains the pelvis, trunk, and bilateral lower-limb segments, with
sagittal-plane hip, knee, and ankle motion. Actuation is provided by 18
Hill-type muscles, corresponding to nine muscle groups per leg: tibialis
anterior (TA), soleus (SOL), gastrocnemius (GAS), vasti (VAS), biceps femoris
short head (BIF), iliopsoas (ILI), hamstrings (HAM), gluteus maximus (GLU), and
rectus femoris (FEM). Foot--ground contact elements are used to compute
vertical ground reaction forces for gait analysis.

\subsubsection{Compared Control Interfaces}
Three DRL policy interfaces were compared. The proposed \emph{Residual Reflex}
controller uses the four-dimensional reflex-parameter action space described in
Table~\ref{tab:residual_actions}. The \emph{End-to-End} baseline directly
outputs muscle-level control actions without reflex modulation. A second
\emph{DEP-RL} baseline uses the same direct muscle-level action space with
DEP-style exploratory feedback. All policies were trained with MPO-based
off-policy reinforcement learning on the same musculoskeletal model.

\begin{table}[!t]
\centering
\caption{Policy interfaces compared in the validation experiments.}
\label{tab:policy_interface}
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\linewidth}{l c X}
\toprule
\textbf{Controller} & \textbf{Action dim.} & \textbf{Policy output} \\
\midrule
Residual Reflex & 4 & Residual modulation of selected reflex parameters. \\
End-to-End & 18 & Direct muscle-level control actions. \\
DEP-RL & 18 & Direct muscle-level control with DEP-style exploratory feedback. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Evaluation Protocol}
Policies were evaluated at three target walking speeds: 0.8, 1.0, and
1.2~m/s. For each trained policy, a representative walking trajectory was
exported as an STO file. All detected gait cycles were normalized to
0--100\% gait cycle and analyzed using the following metrics.

\subsubsection{Speed tracking and stride measures}
To verify that the policies satisfy the commanded locomotion task, we report
realized walking speed, absolute speed error
$|\hat{v}-v_{\mathrm{target}}|$, stride length, stride time, and the number of
analyzed cycles. Lower speed error indicates better task tracking, whereas
stride length and stride time describe the gait strategy used to achieve the
target speed. The stride measures were computed from the detected gait cycles
in each evaluated trajectory.

\subsubsection{Reference-based gait naturalness}
To quantify whether the generated motion follows normative gait-cycle shapes,
we computed reference-envelope scores for pelvic tilt, hip angle, knee angle,
ankle angle, and vertical GRF. The reference envelopes were obtained from the
post-hoc gait-analysis template and provide lower and upper bounds for each
normalized gait-cycle profile; these envelopes were not used during policy
training. For each quantity, left and right gait cycles were time-normalized
and averaged to obtain a mean simulated profile $\bar{y}(x)$, where $x$
denotes gait-cycle percentage. Let $y_{\min}(x_k)$ and $y_{\max}(x_k)$ denote
the lower and upper reference-envelope values at sample point $x_k$. The
normalized envelope violation is
\begin{equation}
e = \frac{1}{N}\sum_{k=1}^{N}
\frac{
\left[y_{\min}(x_k)-\bar{y}(x_k)\right]_+
+ \left[\bar{y}(x_k)-y_{\max}(x_k)\right]_+
}{
\max\left(0.01, y_{\max}(x_k)-y_{\min}(x_k)\right)
}.
\end{equation}
where $[z]_+=\max(z,0)$.
The corresponding score is
\begin{equation}
S = 100 \cdot \mathrm{clip}(1-e,0,1).
\end{equation}
Higher scores indicate that the averaged trajectory remains closer to the
reference envelope; a score of 100 means that the mean profile stays entirely
inside the envelope. The overall gait score is the average of the pelvic, hip,
knee, ankle, and GRF component scores.

\subsubsection{GRF morphology}
Because vertical GRF directly reflects stance loading and push-off behavior, we
also visualize individual left and right GRF gait cycles. This analysis is used
to assess whether the controller produces repeatable stance-phase loading,
reasonable double-support/propulsion morphology, and avoids abnormal impact
peaks. The numerical GRF score is the GRF component of the reference-envelope
metric above, while the cycle plots show left--right and stride-to-stride
consistency.

\subsubsection{Symmetry and repeatability}
Finally, bilateral symmetry was quantified as the root-mean-square difference
between the average left and right normalized profiles. For a gait-cycle
quantity $y$, this waveform difference is
\begin{equation}
D_y =
\sqrt{\frac{1}{N}\sum_{k=1}^{N}
\left(\bar{y}_{L}(x_k)-\bar{y}_{R}(x_k)\right)^2},
\end{equation}
where $y$ denotes a normalized gait-cycle waveform, $x_k$ is the $k$th
gait-cycle sample, $N$ is the number of samples in the normalized gait cycle,
and $\bar{y}_{L}$ and $\bar{y}_{R}$ are the average left- and right-side
profiles computed over the analyzed cycles. In the results, lower $D_y$
indicates more symmetric left--right motion.

Stride-to-stride repeatability was quantified using the coefficient of
variation (CV) of stride time and stride length:
\begin{equation}
\mathrm{CV}_{z}=100\frac{\sigma_z}{\mu_z},
\end{equation}
where $z$ denotes either stride time or stride length, $\mu_z$ is the mean
value of $z$, and $\sigma_z$ is its standard deviation over the analyzed gait
cycles. The factor 100 expresses CV as a percentage. In the results, lower
$\mathrm{CV}_z$ indicates more repeatable gait cycles.

\section{Results and Analysis}
\label{sec:results-and-analysis}
The results are organized according to the evaluation metrics defined above:
speed tracking and stride measures, reference-based gait naturalness, GRF
morphology, symmetry/repeatability, and complete gait-cycle visualization.

\subsection{Speed Tracking and Stride Measures}
Table~\ref{tab:spatiotemporal_metrics} reports realized speed, absolute speed
error, stride length, stride time, and the number of analyzed gait cycles. All
three policies generated forward walking near the commanded speeds, showing
that the comparison is made between policies that can complete the target-speed
walking task. The direct-control baselines generally produced less consistent
stride measures, whereas the residual reflex controller generated longer and
more regular strides. These task-level metrics establish task completion before
gait naturalness is evaluated.

\begin{table}[!t]
\centering
\caption{Speed tracking and stride measures from the analyzed walking
trajectories. Speed error is the absolute difference between realized and target
speed.}
\label{tab:spatiotemporal_metrics}
\small
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{l l r r r r r}
\toprule
\textbf{Target} & \textbf{Method} & \textbf{Real. speed} & \textbf{Error} & \textbf{Stride len.} & \textbf{Stride time} & \textbf{Cycles} \\
\midrule
0.8 & Residual Reflex & 0.79 & \textbf{0.01} & 0.99 & 1.26 & 35 \\
 & End-to-End & 0.87 & 0.07 & 0.78 & 0.89 & 50 \\
 & DEP-RL & 0.83 & 0.03 & 1.09 & 1.31 & 33 \\
\midrule
1.0 & Residual Reflex & 1.05 & 0.05 & 1.23 & 1.17 & 37 \\
 & End-to-End & 1.01 & \textbf{0.01} & 0.87 & 0.87 & 53 \\
 & DEP-RL & 1.14 & 0.14 & 1.29 & 1.13 & 39 \\
\midrule
1.2 & Residual Reflex & 1.21 & \textbf{0.01} & 1.34 & 1.11 & 40 \\
 & End-to-End & 1.26 & 0.06 & 1.23 & 0.97 & 46 \\
 & DEP-RL & 1.30 & 0.10 & 1.37 & 1.06 & 42 \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{figure}[!t]
\centerline{\includegraphics[width=\columnwidth]{spatiotemporal_metrics_bars.png}}
\caption{Speed tracking and stride measures across target speeds. The speed
error evaluates task tracking, while stride length and stride time summarize
the gait pattern used to achieve the target speed.}
\label{fig:spatiotemporal_metrics}
\end{figure}

\subsection{Reference-Based Gait Naturalness}
Table~\ref{tab:gait_similarity} summarizes the reference-envelope gait-cycle
scores for the three policy interfaces. Across the three target speeds, the
proposed residual reflex controller achieved the highest overall
reference-based gait naturalness. The average overall score over the three
speeds was 89.9\% for Residual Reflex, 63.7\% for End-to-End, and 51.3\% for
DEP-RL. Fig.~\ref{fig:gait_score_bars} visualizes the main component
scores and shows that the largest residual-reflex improvements appear in knee
kinematics, ankle kinematics, and vertical GRF morphology. DEP improved the
0.8~m/s end-to-end knee score, but it did not consistently improve overall gait
naturalness, especially at higher speeds where the GRF score collapsed.
These results indicate that completing the speed-tracking task does not
necessarily imply reference-consistent joint and contact dynamics.

\begin{table}[!t]
\centering
\caption{Reference-based gait naturalness scores in controlled walking
validation. Scores are computed from normalized gait-cycle kinematics and
vertical GRF.}
\label{tab:gait_similarity}
\small
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{l l r r r r r r}
\toprule
\textbf{Target} & \textbf{Method} & \textbf{Real.} & \textbf{Overall} & \textbf{Hip} & \textbf{Knee} & \textbf{Ankle} & \textbf{GRF} \\
\midrule
0.8 & Residual Reflex & 0.79 & \textbf{84.2} & 96.6 & \textbf{68.9} & \textbf{78.5} & \textbf{76.7} \\
 & End-to-End & 0.87 & 50.5 & \textbf{98.3} & 18.6 & 0.0 & 35.9 \\
 & DEP-RL & 0.83 & 54.4 & 72.7 & 50.8 & 8.7 & 39.8 \\
\midrule
1.0 & Residual Reflex & 1.05 & \textbf{92.3} & \textbf{100.0} & \textbf{82.6} & \textbf{88.9} & \textbf{89.8} \\
 & End-to-End & 1.01 & 70.9 & 99.9 & 45.4 & 61.5 & 48.0 \\
 & DEP-RL & 1.14 & 55.1 & 62.5 & 64.2 & 48.7 & 0.0 \\
\midrule
1.2 & Residual Reflex & 1.21 & \textbf{93.2} & \textbf{99.6} & \textbf{88.7} & \textbf{88.6} & \textbf{89.1} \\
 & End-to-End & 1.26 & 69.6 & 88.4 & 52.4 & 85.9 & 21.5 \\
 & DEP-RL & 1.30 & 44.5 & 44.5 & 33.4 & 44.5 & 0.0 \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{figure}[!t]
\centerline{\includegraphics[width=\columnwidth]{gait_score_comparison_bars.png}}
\caption{Reference-based gait naturalness scores across target speeds. Numeric
labels indicate the score of each bar. The residual reflex controller achieves
the highest overall score and especially improves knee, ankle, and GRF
components.}
\label{fig:gait_score_bars}
\end{figure}

Figures~\ref{fig:gait_profiles_08}--\ref{fig:gait_profiles_12} show the
normalized kinematic gait-cycle profiles used to interpret these scores. They
show where each controller follows or deviates from the reference envelopes
across pelvic tilt, hip, knee, and ankle profiles. The residual reflex
controller produced compact and consistent left--right gait cycles across
speeds, whereas both direct-control baselines showed larger cycle-to-cycle
variation or larger deviations from the reference envelopes, especially in the
knee and ankle profiles. The smoother residual-reflex kinematic profiles are
consistent with the higher knee and ankle scores in
Table~\ref{tab:gait_similarity}. The GRF component is analyzed separately in the
next subsection.

\begin{figure}[!t]
\centerline{\includegraphics[width=\columnwidth]{gait_analysis_residual_vs_e2e_0p8.png}}
\caption{Reference-envelope kinematic gait-cycle visualization at target speed
0.8~m/s for Residual Reflex, End-to-End, and DEP-RL. Blue and red
curves denote left and right gait cycles, respectively; the black curve is the
cycle average and the gray band is the reference envelope.}
\label{fig:gait_profiles_08}
\end{figure}

\begin{figure}[!t]
\centerline{\includegraphics[width=\columnwidth]{gait_analysis_residual_vs_e2e_1p0.png}}
\caption{Reference-envelope kinematic gait-cycle visualization at target speed
1.0~m/s for Residual Reflex, End-to-End, and DEP-RL. Blue and red
curves denote left and right gait cycles, respectively; the black curve is the
cycle average and the gray band is the reference envelope.}
\label{fig:gait_profiles_10}
\end{figure}

\begin{figure}[!t]
\centerline{\includegraphics[width=\columnwidth]{gait_analysis_residual_vs_e2e_1p2.png}}
\caption{Reference-envelope kinematic gait-cycle visualization at target speed
1.2~m/s for Residual Reflex, End-to-End, and DEP-RL. Blue and red
curves denote left and right gait cycles, respectively; the black curve is the
cycle average and the gray band is the reference envelope.}
\label{fig:gait_profiles_12}
\end{figure}

\subsection{GRF Morphology}
Because ground reaction force reflects how the controller loads and unloads the
body during stance, we further examine the vertical GRF profiles separately in
Fig.~\ref{fig:grf_morphology}. The residual reflex controller produced smoother
stance-phase force profiles that remained closer to the reference envelope
across the tested speeds. In contrast, both direct-control baselines generated
less regular loading patterns, including sharper early-stance peaks, reduced
late-stance support, or force profiles outside the reference envelope. This
difference is consistent with the component scores in
Table~\ref{tab:gait_similarity}, where the residual reflex controller achieved
higher GRF scores than both direct-control baselines at all three speeds.

\begin{figure}[!t]
\centerline{\includegraphics[width=\columnwidth]{grf_morphology_comparison.png}}
\caption{Vertical GRF morphology across target speeds for Residual Reflex,
End-to-End, and DEP-RL. Blue and red thin curves show individual left
and right normalized gait cycles, respectively; the black curve denotes the
cycle mean, the gray band denotes the reference envelope used for post-hoc
evaluation, and the panel annotations report the GRF reference-envelope score.}
\label{fig:grf_morphology}
\end{figure}

\subsection{Symmetry and Repeatability}
Table~\ref{tab:symmetry_variability} reports left--right waveform differences
and stride variability. The residual reflex controller consistently produced
lower left--right RMSE and lower stride time/length CV than both direct-control
baselines. DEP reduced stride variability relative to the end-to-end baseline in
some cases, but it did not improve bilateral waveform symmetry. This supports
the qualitative observation that the residual reflex policy generates a more
regular and bilaterally symmetric gait pattern.

\begin{table}[!t]
\centering
\caption{Cycle-level left--right waveform differences and stride variability.
Lower values indicate more symmetric or more repeatable gait.}
\label{tab:symmetry_variability}
\small
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{l l r r r r r r}
\toprule
\textbf{Target} & \textbf{Method} & \textbf{Hip} & \textbf{Knee} & \textbf{Ankle} & \textbf{GRF} & \textbf{Time CV} & \textbf{Length CV} \\
\midrule
0.8 & Residual Reflex & \textbf{0.80} & \textbf{1.03} & \textbf{0.89} & \textbf{0.04} & \textbf{1.98} & \textbf{5.66} \\
 & End-to-End & 2.92 & 5.75 & 6.36 & 0.10 & 9.05 & 14.88 \\
 & DEP-RL & 11.20 & 11.73 & 11.59 & 0.12 & 7.96 & 11.04 \\
\midrule
1.0 & Residual Reflex & \textbf{1.78} & \textbf{1.85} & \textbf{0.97} & \textbf{0.03} & \textbf{1.51} & \textbf{1.86} \\
 & End-to-End & 2.97 & 4.08 & 2.18 & 0.08 & 6.05 & 8.27 \\
 & DEP-RL & 4.54 & 10.12 & 16.27 & 0.28 & 2.62 & 5.27 \\
\midrule
1.2 & Residual Reflex & \textbf{0.87} & \textbf{0.87} & \textbf{0.46} & \textbf{0.02} & \textbf{0.70} & \textbf{0.74} \\
 & End-to-End & 3.43 & 3.00 & 9.84 & 0.04 & 3.87 & 5.83 \\
 & DEP-RL & 8.33 & 5.83 & 6.31 & 0.34 & 2.29 & 4.78 \\
\bottomrule
\end{tabular}%
}
\end{table}

\begin{figure}[!t]
\centerline{\includegraphics[width=\columnwidth]{gait_symmetry_variability_bars.png}}
\caption{Left--right waveform differences and stride variability across target
speeds. Lower values indicate more symmetric and more repeatable gait cycles.}
\label{fig:symmetry_variability_bars}
\end{figure}

\subsection{Discussion}
The results indicate that the main benefit of residual reflex learning is a
more structured and physiologically plausible gait pattern. In the current
data, end-to-end policies can achieve successful forward walking, yet their
gait-cycle profiles may deviate substantially from reference kinematics and
GRF. This observation suggests that learned musculoskeletal gait should be
assessed not only by task completion, but also through gait-cycle kinematics,
GRF morphology, bilateral symmetry, and cycle-to-cycle repeatability.

The current study has several limitations. First, all reported experiments use
a single training seed for each configuration, so the results should be
interpreted as trajectory-level evidence rather than training-seed statistics.
Second, the current validation focuses on target-speed walking and does not yet
include online speed transitions, external perturbation recovery, or true
metabolic cost estimation. Third, the model is planar, and extension to
three-dimensional locomotion remains future work. These limitations define the
next experimental steps, but they do not affect the central finding that a
low-dimensional residual reflex action space can improve gait plausibility
relative to direct muscle-level control within the present validation setting.


\section{Conclusion}
This letter presented a residual reflex DRL pipeline for musculoskeletal gait
generation. Rather than directly commanding all muscle
excitations, the learned policy modulates four selected reflex parameters
around a reflex-based controller. This formulation reduces the effective action
space and preserves a structured neuromechanical prior within the control loop.

As an initial validation, experiments in a Hyfydy-based planar musculoskeletal
model showed that the residual reflex controller produced gait cycles with
higher reference-envelope gait-cycle scores than an end-to-end muscle-control
baseline at 0.8, 1.0, and 1.2~m/s.
The improvement was most evident in knee kinematics, ankle kinematics, and
vertical GRF morphology. Additional cycle-level analysis showed lower
left--right waveform differences and lower stride variability, indicating more
regular and symmetric gait patterns.

The results support the view that successful walking alone is not sufficient
for evaluating learned musculoskeletal locomotion. Future work will include
multi-seed training statistics, controlled perturbation-recovery tests, and
effort or metabolic-cost analysis using validated biomechanical energy models.

\section*{Acknowledgment}
The authors would like to thank the developers of musculoskeletal simulation
tools used in this work.

\begin{thebibliography}{00}
\bibitem{hyfydy}
Hyfydy, ``Hyfydy: A physics engine for biomechanical simulation.'' [Online].
Available: https://hyfydy.com

\bibitem{geyer2010}
H. Geyer and H. Herr, ``A muscle-reflex model that encodes principles of
legged mechanics produces human walking dynamics and muscle activities,''
\textit{IEEE Transactions on Neural Systems and Rehabilitation Engineering},
vol. 18, no. 3, pp. 263--273, 2010.


\bibitem{deprl2023}
F. Paulus, L. N. Haab, T. Geng, C. Pinneri, N. Reimers, S. Schmitt, and
G. Martius, ``DEP-RL: Embodied exploration for reinforcement learning in
overactuated and musculoskeletal systems,'' in \textit{International
Conference on Learning Representations}, 2023.

\bibitem{residualrl2019}
T. Johannink, S. Bahl, A. Nair, J. Luo, A. Singh, M. Loskyll,
J. A. Ojea, E. Solowjow, and S. Levine, ``Residual reinforcement learning for
robot control,'' in \textit{IEEE International Conference on Robotics and
Automation}, 2019, pp. 6023--6029.
\end{thebibliography}

\end{document}
